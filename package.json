{
  "name": "react-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "lint": "echo 'No linting needed for static HTML'"
  },
  "dependencies": {
    "vite": "^6.2.3"
  },
  "devDependencies": {
    "vite": "^6.2.3"
  }
}
